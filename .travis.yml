language: java
jdk:
  - oraclejdk8
notifications:
  email:
    recipients:
      - business@mircogatz.de
    on_success: change
    on_failure: always
install:
  pip install codecov
after_success:
  - bash <(curl -s https://codecov.io/bash)
